---
import ShareCard from '../../../components/ShareCard.astro';
import { parseShare } from '../../../lib/shareParams';
export const prerender = false;
if (!parseShare(Astro.url)) return Astro.redirect('/es/', 302);
---
<ShareCard lang="es" />
