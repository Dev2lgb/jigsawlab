---
import PuzzleDetail from '../../../components/PuzzleDetail.astro';
import { PAINTINGS } from '../../../lib/jigsaw';
export function getStaticPaths() { return PAINTINGS.map((p) => ({ params: { key: p.key } })); }
const { key } = Astro.params;
---
<PuzzleDetail lang="ja" key={key!} />
